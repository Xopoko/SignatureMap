interface:
  display_name: "Signature Map"
  short_description: "Refresh and query signatures.json before broad code scanning"
  default_prompt: "Use $signature-map as the primary navigation index: refresh the map, query symbols/regex, then open only required source slices."
